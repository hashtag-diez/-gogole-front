export type Book = {
    id: number,
    title: string,
    author: string,
    imagepath: string,
    bc: number,
    dbId: number,
    filepath: string,
    subjects: string
}

export type Completion = {
    id: number,
    word: string
}